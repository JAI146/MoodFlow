# Supabase (required for auth)
# Get these from: https://supabase.com/dashboard/project/_/settings/api
# Use "anon" / "public" key for client-side (never use service_role in the browser)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Optional: Supabase also supports a publishable key (sb_publishable_...)
# NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...

# Supabase Service Role Key (required for seeding - creates users programmatically)
# Get this from: https://supabase.com/dashboard/project/_/settings/api
# WARNING: Never expose this in client-side code!
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Prisma Database URL (following Supabase guide)
# Step 1: Create prisma user in Supabase SQL Editor (see supabase/migrations/20260208000000_create_prisma_user.sql)
# Step 2: Update connection string with prisma user and password
#
# IMPORTANT: prisma migrate dev hangs on the pooler (port 6543). Use Session mode (5432) for DATABASE_URL
# when running migrations locally, or set DIRECT_URL to the direct connection and keep DATABASE_URL as pooler.
#
# For server-based / migrations - use Session mode (port 5432):
DATABASE_URL="postgresql://prisma.your-project-ref:your-prisma-password@aws-1-us-east-1.pooler.supabase.com:5432/postgres"

# For serverless (Vercel, etc.) use Transaction mode (6543) in DATABASE_URL and set DIRECT_URL for migrations:
# DATABASE_URL="postgresql://prisma...@aws-1-us-east-1.pooler.supabase.com:6543/postgres?pgbouncer=true"
# DIRECT_URL="postgresql://prisma...@aws-1-us-east-1.pooler.supabase.com:5432/postgres"

# Direct connection - used by Prisma for migrate dev (avoids hang). Use port 5432, same credentials as DATABASE_URL.
DIRECT_URL="postgresql://prisma.your-project-ref:your-prisma-password@aws-1-us-east-1.pooler.supabase.com:5432/postgres"

# Optional: If you already have a user and want to seed data for that user
# DUMMY_USER_ID=your-existing-user-uuid
